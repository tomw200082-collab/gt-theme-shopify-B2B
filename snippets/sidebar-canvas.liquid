{%- render 'sidebar-cart' with enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
{%- render 'sidebar-mobile-nav' with enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
{%- render 'sidebar-login-form' -%}