{%- if block.settings.text_align_mobile != 'left' or block.settings.text_align != 'left' -%}
.footer__img-align-{{ block.id }}{margin:{%- if block.settings.text_align == 'left' -%}0 auto 0 0{%- elsif block.settings.text_align == 'center' -%}0 auto{%- else -%}0 0 0 auto{%- endif -%};}
{%- endif -%}